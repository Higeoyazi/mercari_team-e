= form_for @user, url: step4_signup_index_path, method: :get do |f|
  .user-container#step3
    %header.header__registration
      .header__registration__box
        %h1
          = link_to "" do
            =image_tag "//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?1881812696", alt: "mercari"
        %nav.registration__stage
          %ol
            %li
              会員情報
              .registration__stage__bar
            %li.current-position
              電話番号認証
              .registration__stage__bar
            %li
              お届け先住所入力
              .registration__stage__bar
            %li
              支払い方法  
              .registration__stage__bar
            %li#registration__finish
              完了
              .registration__stage__bar
    %main.main__tel
      %h2.title__tel
        電話番号の確認
      .inner__tel
        .inner__tel__box
          %label.inner__tel__box__number
            携帯電話の番号
          = f.fields_for :profile do |p|
            = p.text_field :phone_number, autofocus: true, class:"form__input",placeholder:"携帯電話の番号を入力"
        %p.text_use_sms
          本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
        = f.submit :SMSを発信する ,{class:"form-button",id:"button__step2"}
        %p.text__oath
          ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
        .inner__tel__text__right
          = link_to '電話番号の確認が必要な理由',"" 
          = fa_icon 'fa chevron-right', id: 'icon__chevron-right'
    = render "shared/devise-footer"